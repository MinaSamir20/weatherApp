import { TempertureConversionPipe } from './temperture-conversion.pipe';

describe('TempertureConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new TempertureConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
